[lint]
select = ["E", "F", "W"]
# E501: line-too-long — black handles line length
# E402: module level import not at top of file — required for safe_import pattern in skill modules
# E741: ambiguous variable name
ignore = ["E501", "E402", "E741"]

[lint.per-file-ignores]
"tests/*" = ["F841"]
