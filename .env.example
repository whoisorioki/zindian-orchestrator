# Example environment variables — DO NOT COMMIT SECRETS HERE
# Copy this to `.env` and fill with real secrets. NEVER commit your real `.env`.

# Semantic Scholar S2 API key (set by user; required for librarian/code-miner skills)
SEMANTIC_SCHOLAR_API_KEY=

# Optional keys used by research sidecar / cloud clients
GEMINI_API_KEY=
ANTHROPIC_API_KEY=
GITHUB_TOKEN=
ZINDI_USERNAME=
ZINDI_PASSWORD=
