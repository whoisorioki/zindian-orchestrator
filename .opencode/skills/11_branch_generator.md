---
description: "Skill 11 — Branch Generator"
---

## Goal
Generate hypothesis-driven branches (feature sets / CV changes / model params) that remain compliant with `challenge_config.json`.

## Output
- Branch plan entries in `competitions/<slug>/reports/experiments.json` with names + rationale.

