---
description: "Skill 12 — Metric Trade-off"
---

## Rules
- Only run metric trade-off analysis when it makes sense for the metric type (read from `challenge_config.metric`).
- Never change objective/target semantics beyond what rules permit.

